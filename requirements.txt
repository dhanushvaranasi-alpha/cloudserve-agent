# Pinned dependencies for the CloudServe support system.
# Deviates from the pack's template requirements.txt (which pinned very old
# releases, e.g. chromadb==0.3.21, langchain==0.1.0, that no longer install
# cleanly). This is a documented engineering decision -- see docs/architecture.md
# "Stack decisions" -- everything below is still free/open source.

# LLM access (Groq free tier, OpenAI-compatible)
groq==0.11.0

# Vector store & embeddings
chromadb==0.5.20
sentence-transformers==3.2.1

# Data / eval
numpy==1.26.4
scikit-learn==1.5.2

# Web framework
fastapi==0.115.4
uvicorn==0.32.0
pydantic==2.9.2

# Reliability
tenacity==9.0.0
python-dotenv==1.0.1

# Monitoring
prometheus-client==0.21.0

# Testing & quality
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Utilities
tqdm==4.66.5
