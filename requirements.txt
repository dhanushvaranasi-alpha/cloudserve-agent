# Pinned dependencies for the CloudServe support system.
#
# This branch (langchain-langgraph-migration) replaces the plain-Python
# control flow and the direct groq/chromadb clients with LangChain and
# LangGraph. See docs/architecture.md "Stack decisions" for why the
# original branch (master) chose plain Python instead, and
# docs/PRD_REVISION_LOG.md for why this branch exists anyway.
#
# The original template pinned very old releases (e.g. chromadb==0.3.21,
# langchain==0.1.0) that no longer install cleanly; everything below is a
# current, freely installable version instead.

# Orchestration
langgraph==1.2.11
langchain==1.4.1
langchain-core==1.6.3

# LLM access (Groq free tier, OpenAI-compatible), via langchain-groq
langchain-groq==1.1.3
groq==0.37.1
httpx==0.27.2
# Pinned: older groq releases call httpx with a `proxies` argument that
# httpx 0.28+ removed. Confirmed still required with the newer groq pulled
# in by langchain-groq's own dependency resolution.

# Vector store & embeddings, via langchain-chroma / langchain-huggingface
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
chromadb==1.5.9
sentence-transformers==3.2.1

# Data / eval
numpy==2.1.3
# Pinned to a 2.x release: langchain-chroma requires numpy>=2.1.0 on
# Python 3.13 specifically (pip's resolver backtracks for a long time
# and then fails outright against the old numpy==1.26.4 pin from master's
# requirements.txt). 2.1.3 is still compatible with scikit-learn==1.5.2,
# sentence-transformers==3.2.1 and Python 3.10-3.13, so one pin covers
# every environment this project runs in.
scikit-learn==1.5.2

# Web framework
fastapi==0.115.4
uvicorn==0.32.0
pydantic==2.9.2

# Reliability
tenacity==9.0.0
python-dotenv==1.0.1

# Monitoring
prometheus-client==0.21.0

# Testing & quality
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Utilities
tqdm==4.66.5
